
<% if user_signed_in? %>
 <% if manager? %>

<table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">

    <tr>

      <th>Title</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
     <%# <% if project.workin_ons.user == current_user%>

      <tr>
        <td>
         <%= link_to project.title , project %>
        </td>

        <td>

        <%= link_to 'Users', all_users_projects_path(project.id), class:"btn btn-dark" %>
        <%= link_to 'Bugs', project, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-dark" %>
        <%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>


      </tr>
    <% end %>
  </tbody>
</table>





<br>
 <% elsif qa? %>
  <table class="table table-striped table-bordered table-hover">
  <thead class="thead-dark">

    <tr>

      <th>Title</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
     <%# <% if project.workin_ons.user == current_user%>

      <tr>
        <td>
         <%= link_to project.title , project %>
        </td>

        <td>
        <%= link_to 'Report a Bug', project, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-dark" %>
      </tr>
    <% end %>
  </tbody>
</table>
 <% end %>
<% else %>
 <h1>Hello Rails alii</h1>
<% end %>

