<%= form_with(model: project, local: true, remote: true) do |form| %>
  <div class="field form-group">
    <%= form.text_field :title, class:"form-control", placeholder:"Title", required: true  %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
